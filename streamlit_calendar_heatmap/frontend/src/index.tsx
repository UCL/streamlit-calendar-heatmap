import React from "react"
import ReactDOM from "react-dom"
import StreamlitCalendarHeatmap from "./CalendarHeatmap"

ReactDOM.render(
  <React.StrictMode>
    <StreamlitCalendarHeatmap />
  </React.StrictMode>,
  document.getElementById("root")
)
